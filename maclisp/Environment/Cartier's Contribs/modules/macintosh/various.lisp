;;; -*- package: CC -*-;;;;;;; Varia macintosh;;;(in-package "CC")(defun function-enqueue (function)  (eval-enqueue    (list 'funcall function)))(defmacro lexical-enqueue (&body body)  `(function-enqueue     (function       (lambda ()         ,@body))))(defun picture-size (pict)  (make-point    (- (rref pict picture.picFrame.right)  (rref pict picture.picFrame.left))    (- (rref pict picture.picFrame.bottom) (rref pict picture.picFrame.top))))(defun modifiers ()  (let ((modifiers nil))    (when (command-key-p) (push :command modifiers))    (when (option-key-p)  (push :option  modifiers))    (when (control-key-p) (push :control modifiers))    (when (shift-key-p)   (push :shift   modifiers))    modifiers))