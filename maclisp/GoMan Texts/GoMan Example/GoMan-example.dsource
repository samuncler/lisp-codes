@Chapter{Aktion}@Label{ "label-1" }Der File 'GoMan-example.lisp' enthält ein kleines Beispiel wie einGoMan-Text mit einer Lisp-Anwendung verbunden werden kann.@Example{Beispiel Code}Das folgende Codestück kann durch das Copy-Icon in den Listenerkopiert werden. @Verbatim          (ed           (format nil               "~aGoMan Texts:GoMan Example:GoMan-Example.lisp"               (ccl::startup-directory)))@End-VerbatimEs sollte ein 'Eval Buffer' ausgeführt werden.@End-Example  Danach kann eine @Application-Link{ "Aktion" | "do-it" } in derAnwendung ausgelöst werden. @Chapter{Application-Link}@Label{ "label-2" }Das ist ein @Application-Link{ "Application-Link" | "item-name" } aufein Fenster mit dem Namen "item-name".Die Gross-Kleinschreibung ist zu beachten.@Chapter{CTRL-Click}@Label{ "label-3" }Durch einen CTRL-Click auf das Anwendungsfenster erscheint einPop-Up-Menü mit Verweisen auf interessante Textstellen. @Chapter{Eine Grafik}@Label{ "label-4" }Grafiken werden im Resource-File abgelegt und mit demPicture-Archive (siehe Eintrag im Special-Menü) verwaltet.@Figure{ "Geklaute Grafik" | "128" | 360 | 178 }Grafiken werden in einer anderen Applikation erstellt und durch"Copy-Paste" in das Picture-Archive kopiert. Die Integration der Grafikerfolgt durch eine Drag-And-Drop-Aktion der Grafik an die gewünschteTextstelle innerhalb des Textbereichs. @Chapter{Speichern des Manuals}Das fertig gelayoutete Manual wird durch eine Drag-And-Drop-Aktiondes Fensters auf das entsprechende Bookshelf-Icon gespeichert. 